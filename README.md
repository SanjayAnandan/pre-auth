1. Download requirements

Run in terminal ->> pip install -r requirements.txt

2. Generate an API key in Groq or anything else and store the API key in .env file

3. Run the file using the below command

python -m streamlit run app.py